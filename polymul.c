#include <stdio.h>
#include<stdlib.h>
int* polymul(int a[],int b[],int s,int e){
  int mid=s+(e-s)/2;
  int* res=(int*)malloc(sizeof(int)*(e-s+1));
  
}
int main(){
    int n;
    scanf("%d",&n);
    int p1[n];
    for(int i=0;i<n;i++){
      scanf("%d",&p1[i]);
    }
    int p2[n];
    for(int i=0;i<n;i++){
      scanf("%d",&p2[i]);
    }
    
   
}